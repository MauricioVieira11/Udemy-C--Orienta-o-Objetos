using System; //namespace padrão do dotnet

namespace Primeiro
{
    class Program
    {
        static void Main(string[] args) //aplicação padrão de ponto de entrada(entrepoint)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
